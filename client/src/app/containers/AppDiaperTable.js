import React from 'react';
import {connect} from 'react-redux';
import autobind from 'autobind-decorator';

export class AppDiaperTable extends React.Component {
    render() {
        return (
            <div className="diaper-wrapper">work in progress...</div>
        );
    }
}